# 2026 Q3 — Plan (Jul–Sep 2026)

> Forward-looking. Drawn from the open work-streams at the end of Q2 — order and scope
> will drift. "Sound first" still wins ties: engine work takes priority over launch/marketing.
>
> **Progress (end of Q2):** the multi-engine backend foundation + the FE/BE state-placement cleanup
> landed **early, in Q2**. What's left from the headline is the **master / loudness stage** (the original
> feature) + the **warehouse pool**; the Engine-DSL app path and the worklet clock fix are still open.
> Detail + status: `docs/tasks/per-playback-engine.md` and memory `project_fe_be_state_placement`.
>
> **Progress (mid-Q3, 2026-08-12):** act one is essentially done. The **master / loudness stage**
> shipped as *master-in-pattern* (2026-08-03) plus reverb/delay **parameter parity**; the master
> limiter gained **lookahead** (2026-08-11). Unplanned but sound-first: the **unison phase pool**
> (2026-08-12, v0.2.0) killed the super-osc "fundamental lottery", and **block-size parity** + a
> **sample-onset fix** (2026-08-08) made offline renders match live playback. The **audio-backend
> audit** was scoped (2026-08-04) — must-do before launch. Act two (tutorials) not started yet.
> Current status lives in `docs/tasks/_priorities.md`.

Roughly two acts. **First:** finish and polish the engine — the master / loudness stage and the
Engine DSL. **Then:** with a grown, polished engine under us, this becomes the **quarter of tutorials** —
building out new ones and, above all, fixing the current set.

## Multi-engine audio backend (finish what Q2 started)

The headline. Today the backend is one singleton — every playback mixes into one
`Cylinders` + one hardcoded master limiter, which already lets two songs on the same
orbit corrupt each other's FX tails.

- ✅ **DONE (Q2).** Per-`playbackId` **PlaybackEngine** behind a **PlaybackEngineDispatcher**;
  each engine owns its full render state (scheduler + cylinders + render context), no shared
  scratch → multi-thread ready. Plus `AudioBackendContext` + `BackendClock` (shared state + one
  timeline; fixed first-notes-lost).
- ✅ **DONE (2026-08-03) — the actual feature.** Shipped as **master-in-pattern**:
  `master(Master.of(MasterFx.gain(2.5), MasterFx.limiter()))` rides the voice stream, crossfades on swap, works offline;
  `Master.default()` switches it back off. Plus reverb/delay **parameter parity** — the same word now means the same
  thing on orbit and master buses.
- ✅ **Master limiter lookahead (2026-08-11)** — the limiter sees peaks coming instead of cracking transients; the voice
  timeline widened to `Double` along the way.
- ✅ Mixdown fast path: 1 engine renders straight into the mix (the ≥2-engine shared scratch lands
  with the master stage).

## Resource warehouse pool (the alloc-spike killer)

- Self-balancing pool for expensive per-engine resources (the ~7.68 MB delay rings,
  cylinders). Keep N ready, warm up ahead, refill/shrink ±1 per render cycle.
- Kills the first-note allocation spike (the "Der Schmetterling" stutter) and caps memory.
- Deliberately scheduled **last** in the backend work, after the FE/BE cleanup.

## Engine DSL — finish + extend

- ✅ Application path: inline `.pipeline(...)` wired end-to-end (PR #64).
- ✅ Renamed `EngineDsl` → `PipelineDsl` ("engine" belongs to the PlaybackEngine layer).
- ✅ Phase 2 typed oscillator subtypes (PR #64); Adsr `declickSeconds`/`expK` feel-knobs landed as oscParam slots
  (2026-07-04). ⬜ Filter feel-knobs + Phase 3 `EngineTuning` profiles still open.

## Landed unplanned (sound first)

- ✅ **Unison phase pool (2026-08-12, v0.2.0)** — banded start-phase selection + per-orbit pools kill the super-osc
  "fundamental lottery" (onset holes 16 % → 0, never-rings 6/120 → 0, texture untouched). OFF by default; per-song
  opt-in via one combined `.phasePool(...)` call. Written up as a blog post
  (`docs/blog/2026-08-12-the-fundamental-lottery`).
- ✅ **Block-size parity + sample-onset fix (2026-08-08)** — 128 frames pinned everywhere (block size is a *sound*
  parameter); sample voices no longer start up to a block late. Offline = live.
- ✅ **White paper (2026-08-09)** — `docs/whitepaper/klang-whitepaper.html`.
- ⬜ **Audio-backend audit** — scoped 2026-08-04 (`docs/tasks/audio-backend-audit.md`): the green
  `audio_be` suite is an unmeasured net (no mutation checks); hand audit, `voices/` pilot first. SHOULD now, MUST before
  launch.

## The quarter of tutorials (act two)

⬜ Not started as of mid-Aug — act one (above) took the first half of the quarter.

Once the master stage and the Engine DSL are fully ready and polished, the rest of the
quarter goes here. The engine has grown a lot — the tutorials haven't kept up.

- **Fix the current set first.** Most of them have ugly-sounding examples and don't get
  to the *core* of what they're teaching.
- **Build a stringent through-line.** Right now the path from the basics (a plain saw)
  up to a fully crafted sound isn't coherent — each tutorial should be a clear step along
  that line, not a scattered demo.
- Then **build out** new tutorials on top of that spine, showing off the grown engine
  (Engine DSL, body/vowel, drift, the master stage).
- Lean on the music-writing / recording skills and the tutorial factory to keep the
  examples actually musical (sound first).

## FE/BE state placement

- ✅ **DONE (Q2).** Mirror backend granularity on the frontend (playbackId-bound vs global):
  custom-osc / engine state → per-playback (leak fixed), FE↔BE clock offset → `KlangPlayer`
  (drift + first-notes fixed), dead-message cleanup, diagnostics emission → dispatcher (stuck
  gauges fixed).
- ⬜ Worklet **clock divergence** fix (worklet reports wall-time / re-anchor baseTimeMs) — deferred.

## Likely, lower priority

- **Katalyzers** — per-orbit effect chains, the counterpart to per-voice Ignitors.
  The PlaybackEngine work opens the natural home for these.
- Sprudel **constant-control fast-path** (designed, optional perf).
- Ongoing: by-ear sound tuning, more songs & tutorials.

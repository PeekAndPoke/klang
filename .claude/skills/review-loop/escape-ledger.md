# The escape ledger

One row per escaped defect: every CRITICAL or MAJOR a review round found, what let it through, and
the ONE thing that changed so the class cannot escape the same way again. Split out of `SKILL.md`
2026-09-20 so that reading the review standard costs a page instead of ten; the standard's
"Standard 3" states the method and the five classes, this file holds the record.

**Who appends:** the coordinator, when a step commits. **What a row needs:** the date, the step and
round, what escaped, the class (brief, checklist, test, design, tooling, or "recurrence" naming the
row it repeats), and what it changed. A row whose last column is a restatement rather than a
changed artefact is not closed.

**Why recurrence is the signal.** The sample is too small to estimate a rate, so the loop improves
at n = 1: a class that escapes twice means the rule's TEXT failed, and it gets rewritten rather
than repeated. Reading this file for recurrences before writing a brief is the point of keeping it.

**The ledger** (one row per escape, appended when the step commits; the classes above):

| date | step | the escape | class | what it changed |
|---|---|---|---|---|
| 2026-09-17 | Katalyst 2, 3c | em-dashes in moved lines reached two commits | tooling | `git diff \| grep '^+' \| grep -c '—'` before every commit |
| 2026-09-17 | Katalyst 3a | the single NaN probe was defeated by `safeOut` | test | the two-probe coercion and its spec |
| 2026-09-18 | Katalyst 3b | the outgoing chain's sends were unramped; the duck was not carried across a swap | checklist | the audio reviewer template asks "what happens to every send and to the envelope state during the swap" |
| 2026-09-18 | Katalyst 5a | `Katalyst(k => k.classic())` content-equal to classic resolved to the voice-driven chain, `katp` inert | design | the rule "only the born-with chain is voice-driven", decided before 5a's fix round |
| 2026-09-18 | Katalyst 5a | per-door map allocation on the query path | checklist | "allocation per event on the query path" in the coding reviewer template |
| 2026-09-18 | Katalyst 5a-2 | classic's `body.wet` was a SET 0.0, so a material-only `body("wood")` on a declared chain was dry | design + checklist | the compound-door fill rule (rules register, `/dsl-design` §4, checklist 11) and the defaults-sync spec's unset family |
| 2026-09-18 | Katalyst 5a-3, round 1 | the coordinator's rule text named `wet` as the gate of the sends and said the gate is never invented, which the blueprint `reverb(size = 4)` contradicts; the next implementer would have deleted the send fill | brief (the rule author did not read the blueprint before writing the rule) | the register row and `/dsl-design` §4 state the gate per stage kind (name knob vs any knob); rule: a rule that names a blueprint is checked against the blueprint's own spec before it lands |
| 2026-09-18 | Katalyst 5a-3, round 1 | the fills handed `setOrDefault` the voice field, so a `katp` between two calls of the same door was overwritten | checklist | `/dsl-design` checklist 12: the value handed to `setOrDefault` is what this call named, never a field an earlier fill wrote |
| 2026-09-18 | Katalyst 5a-3, round 2 | the corrected rule text listed the any-knob stages as "the sends and the compressor" and omitted the phaser, which the same round brought under the rule; an implementer reading the closed list would delete the phaser fill | recurrence of the round 1 text escape, one round later (the rule enumerated stages without saying the list was closed, so an addition elsewhere in the same change did not update it) | §4 now marks both lists CLOSED and complete and says a new bus door joins one of them in the same change; checklist 11 repeats it. Lesson for the rule author: an enumeration in a rule is a contract, write "closed" or do not enumerate |
| 2026-09-18 | Katalyst 5a-3, round 3 | the duck door filled its companions on ANY knob (since step 5a), while the rule and the body door say a name-knob stage fills only when its name knob is named; audible on a custom chain (`duck(attack = 0.3)` wrote `duck.depth = 0.0` over a chain-authored 0.8); two guards in one spec encoded opposite readings of the same clause | checklist (no review had checked every door against the rule; the strongest-tier reviewer built a door-by-door table against the two closed lists and found it at once) | the duck fills only when THIS call named an orbit; `/review-loop`: when a change lands a rule with a closed list, the reviewer brief asks for the door-by-door table |
| 2026-09-18 | Katalyst 5a-3, rounds 1 to 3 | the same rule text escaped three times in one step, each correction reaching only some of a dozen copies (register, skill, `ParamBag`, the classic KDoc, two constants headers, seven door KDocs, `MEMORY.md`) | structural (a rule restated at every site that obeys it cannot be corrected atomically) | `/dsl-design` §4 is the ONE home of the rule's text; every other site states only its own facts and points there; enumerations of "which stage is in which list" are never copied |
| 2026-09-18 | Katalyst 5a-3, round 3 | byte-identity claims rested on renders that did not exercise the change: neither frozen song calls the phaser, and one render predated the last batch | test (the evidence audit: which render, made when, backs which claim) | before a step commits, every byte-identity claim names a render made on the final tree that exercises the changed door; a changed door no frozen song calls gets a before/after render of a song that does |
| 2026-09-18 | signal-flow 2a, rounds 1 and 2 | an ANALYSIS of a tree (which `Param` names it places) disagreed with what the BUILD creates: first through `Variants` (the union over variants while the build picks one), then through two arms that skip a child (`Detune`'s identity fold, the plain-sine branch). Each time the engine took the consumed row for a leaf that did not exist and the player's touch landed nowhere, a soft note at full level | design (a second walk that PREDICTS what a first walk will do can drift from it wherever the first is conditional) | the analysis mirrors the build's three conditional sites through ONE shared predicate, and a property row over the fuzz corpus asserts "names the build resolves == names the analysis claims" per index, so a future conditional arm turns a test red instead of losing a factor. Rule: when code predicts another walk's outcome, a property test ties the two together |
| 2026-09-18 | signal-flow 2a, round 1 | a new, un-memoized recursive tree walk was placed on the registration path, which in the browser is the audio thread's message handler, directly under a comment explaining why the neighbouring walk dedups by identity | checklist | the audio reviewer template asks of every new walk: which thread runs it, is it linear in DISTINCT nodes, and is its work bounded (a cap on a count is not a cap on work) |
| 2026-09-18 | signal-flow 2a | the plan stated as fact that "the voice factory already collects every Param a tree references"; it did not, and the brief inherited the claim | brief (a plan sentence about what code already does was never checked against the code) | a plan's claim about EXISTING code is verified by grep when the step that depends on it is briefed; the implementer's report opens with "what I found by reading", which is where this was caught |
| 2026-09-18 | found in Katalyst 5a-3 round 3, fixed as its own step | a NaN body or vowel wet from the wire made `KatalystBodyEffect.configure`'s compare against its stored mix true forever: two filter banks allocated per block on the audio thread, the crossfade never completing, and the body inaudible; the nullable floor had the same defect (a `Double?` compare is IEEE on both the JVM and Kotlin/JS, measured) | checklist (pre-existing since the effect was written; no review had asked what a cached config does with a non-finite input) | both effects substitute at the entry of `configure` and compare and store the substituted values; the gain stage got the same guard at its door; the rule "a cached config is reviewed for the non-finite input" above; the first sweep found every other stage safe |
| 2026-09-18 | Katalyst 5a-3, round 1 | `(a == b) shouldBe false` recurred in a new spec one round after it was retired at three sites | recurrence (the rule lived only in a round's findings, nowhere an author reads) | `/code-style` §23: `shouldNotBe`, except on a boxed NaN where the raw form is deliberate |
| 2026-09-19 | signal-flow 2, levels on the wire, round 1 | the new render row compared the engine against itself (`render(0.5)` against `render(1.0) * 0.5`): a scaling law, green with the retired multiplier put back at unity or with a constant trim added; and it built its voice with the test helper, so the factory guard was never in a rendered path | checklist (the brief asked for "a reference" without saying INDEPENDENT of the code under test) | a bit-identity row names its oracle: computed from the test signal's definition and the stage's own association, or frozen bits; a row whose two sides both come from the code under test is a law, not an identity, and says so. A guard is rendered through the path that contains it |
| 2026-09-19 | signal-flow 2, levels on the wire, round 1 | both blind reviewers raised the maintainer's own uncommitted by-ear edits in a song file as the step's defects (one as CRITICAL); the coordinator had told one reviewer and not the other, and neither had evidence to tell the two apart | brief (a working tree that carries someone else's uncommitted work was handed to a blind round without a baseline) | before a step starts in a tree with foreign uncommitted edits, the coordinator saves `git diff -- <file>` to the scratchpad as the baseline, and EVERY reviewer brief names the file, the baseline path and what in it belongs to the step |
| 2026-09-19 | signal-flow 2, the slot and the bus fader, round 1 | a general finite guard at the `Param` leaf made a whole class of input unreachable, and two existing optimizer rows that fed an infinity and a NaN through `oscParams` kept passing while testing the slot's default instead (one of them the guard for a shipped RNG-stream bug class) | brief (it asked whether any SLOT relied on a non-finite value arriving, not whether any TEST did) | a change that makes a class of input unreachable finds the tests that feed it BEFORE it lands, and the brief says so; a repaired row proves engagement again, because green is what the broken row was too. Round 2 showed that a grep is not the method: a third voided row carried the value in a loop variable. The method that cannot miss is a tripwire: make the newly unreachable input throw or record at the guard, run the full suites, read the list, restore |
| 2026-09-19 | signal-flow 2, the slot and the bus fader, round 1 | the step deliberately broke the invariant "the born-with chain ignores `katalystParams`" for one slot, and five sites kept stating it, among them the `katp` door popup, whose advice (declare a chain) would have cost the author a sound change to reach a fader they already had | recurrence (a rule restated at every site cannot be corrected atomically: the 5a-3 lesson, one step later) | when a step breaks a stated invariant, the brief names the invariant's sentence and the implementer greps for its restatements; the sentence gets ONE home and the other sites point at it |
| 2026-09-19 | signal-flow 2, the slot and the bus fader, rounds 1 to 3 | a reviewer's remark about existing code ("a `FilterDef` q becomes a `ParamIgnitor` via `svf` and reaches `scaledBy`") was taken as fact by the coordinator and the implementer, written into a fix round's KDoc and copied to five sites, the plan among them; no production caller exists | recurrence (the 2026-09-18 rule "a claim about EXISTING code is verified by grep" covered plans and briefs, not fix-round comments and not reviewers' findings) | a claim about what existing code DOES is verified by a caller search whoever makes it: plan, brief, reviewer or fix round. A fix batch that asks for such a sentence says "verify before you write" |
| 2026-09-19 | Katalyst 5b-3, bus fields off the wire, round 1 | a removal step left nine comments and KDocs naming the code it deleted (`VoiceFactory.orDefault`, `Voice.Delay`, `normalizeSize` "in VoiceFactory" under a drain-termination proof); both blind reviewers found them, nothing else | new class | a step that DELETES a symbol greps for its name in comments and KDoc too, not only in code, and fixes every mention before review; the compiler catches the code, nothing catches the prose |
| 2026-09-19 | Katalyst 5c-7, the compressor, round 1 | "knob jumps to the floor" rested on a 2 to 8 ratio sweep; the ratio glided linearly in the ratio while the gain law is linear in `1/ratio - 1`, so 20 to 1 and 100 to 1 still clicked at -41 to -56 dB; the blind audio reviewer's Python copy of the path found it | recurrence (a measured claim generalised beyond the swept dimension) | a glide is built on the axis the law is LINEAR in, found by reading the law, and its measurement sweeps each knob's FULL range in both directions, extremes included; a brief that asks for a knob measurement names the range |
| 2026-09-20 | Katalyst 5c-10, the resonator morph, round 1 | two of the seven listening pairs were BIT-IDENTICAL for their whole musical content: the orbit lease hands over on alternating notes, so a `<wood glass>*8` material handed the owner wood every time and nothing ever changed. The same class was caught once in 5c-9 (a `duck.depth` edge the lease never reached) and fixed only in that case's fixture | recurrence | a render that is supposed to SHOW a difference is not shipped until it has been shown to differ: compare the pair (or the before and after) and report the first differing sample. A `katp` or material edge only reaches a stage when the LEASE turns over, so a case must be written so the lease turns over at the edge |
| 2026-09-20 | phase 3 step 2, the gate, round 4 | the implementer reported round 2's orphaned-KDoc MINOR as fixed; the edit had moved the block to a position that orphans the class KDoc just as before, and round 3's review did not catch it either. It surfaced only when a file-changed notice put the header back in front of the implementer | new class | a reported fix is not a landed fix: after applying a batch, RE-READ the thing that was wrong, not the patch that was written. A clean review round is not evidence that a previous round's fix landed, because a reviewer reads the current state as a whole and a small prose regression hides in it |
| 2026-09-20 | Katalyst 5c-11, rounds 1 and 2 | the maintainer LISTENED and passed a set whose "before" side was the wrong code in seven of eight pairs, and the corrected set still could not expose the question it was built for: the artifact sat under the note's own bass and under the attack it coincided with, and the loudest material pairs were in no file at all. Both were found by MEASURING the shipped renders, not by reading the code | new class | a listening set is EVIDENCE and gets the same scrutiny as a test: name what the before side actually is (verify it against that commit, not against the plan), prove each pair differs, and measure that the artifact under question is ABOVE the render's own floor for it. A case that cannot expose the question is worse than no case, because a "no" from it gets recorded as a result |
| 2026-09-20 | phase 3 step 3a, the filter knobs, rounds 1 to 3 | every round found a GUARD gap, never a logic error, and always on the arm or the surface the author was not thinking about: a door whose stage knobs named nothing, a claim corrected on the node and not on the script door, a refusal conjunct pinned on one of four arms. The step's own comparison harness also lied twice, both times because a `sed` that strips the wall-clock seconds was re-typed inline | recurrence (the 2026-09-20 "a reported fix is not a landed fix" row, widened) | when a change touches a FAMILY (four filter kinds, three door forms, two conjuncts), the brief says sweep the family and the rows LOOP over it; a row that covers one member leaves the others deletable with every suite green. And a comparison is evidence, not a line of the script that produced it: the first time one is needed twice it gets a name and a file |
| 2026-09-19 | signal-flow 2, the slot and the bus fader, round 2 to 3 | a measured claim was generalised beyond the dimension that was swept: drive was swept at the default shape, and the prose said "a heavily driven sound" for all sixteen shapes; on the three wavefolders the opposite holds (pregain is the strongest tone knob, and turning it down can make the note louder) | checklist | a user-facing sentence that rests on a measurement names what was held fixed, and the audio reviewer asks which other dimension of the door (shape, mode, passes) could falsify it |
| 2026-09-19 | Katalyst 5b-1, before round 1 and round 1 | the COORDINATOR accepted a measured sound difference as "a note for the listening checkpoint": an explicit `wet = 0` on the orbit's owner now switched a send stage off. The measurement (bit-identical on the way off, no click, -19 dB on the way back) was right and beside the point: the door's own KDoc promises "a dry voice on a wet orbit stays dry", another voice on the orbit lost its room, and which voice owns the orbit is stack order | coordinator judgement (a difference was weighed by how loud it is, not by which promise it breaks) | a difference found in an identity step is judged against the DOCUMENTED contract of the door it touches before it is judged by ear or by decibels; if it breaks a promise, the step restores the old rule and the change moves to the step that carries the listening checkpoint |
| 2026-09-19 | Katalyst 5b-1, round 1 | a shortcut (a declared chain with classic content resolves to the born-with chain) made three render rows OUTSIDE the diff vacuous: they compared "with a classic declaration" against "without", which became one chain object compared with itself, among them the row that had caught step 5a-2's MAJOR | recurrence of the same day's class (a change that makes two things identical, or an input unreachable, defuses tests that are not in the diff) | the implementer's brief for any change that UNIFIES two paths or short-circuits a comparison asks: which existing tests compare those two things, and what do they compare after the change? Found by reading hits, not by grep alone |
| 2026-09-19 | Katalyst 5b-1, round 2 | two test fixtures moved from a `VoiceData` field to a slot and multiplied the reverb size by ten; the round 1 reviewer had verified "every conversion" under the assumption that every old fixture held the NORMALIZED size (`Voice.Reverb.size`), which is true for some and false for the ones built from `VoiceData.reverbSize` (authored scale) | test (a bulk rewrite of fixtures was checked against a rule of thumb, not against each old line) | a hand rewrite of fixtures is reviewed against `git show HEAD:` line by line for UNITS, and the implementer's report lists each converted quantity with the scale of its old and its new home |
| 2026-09-19 | Katalyst 5b-1, round 2 | a documentation fix made in round 1 vanished in round 2: a mutation check on that file was restored from git instead of from a copy taken just before the mutation, which also restored the unfixed text | tooling | a mutation is restored from a copy taken immediately before it and verified with `cmp`; never `git checkout` on a file that carries uncommitted work. The coordinator diffs the FILE LISTS of consecutive rounds' packs: a file that drops out of the diff is a lost edit until shown otherwise |
| 2026-09-19 | Katalyst 5c-1 (the delay as a state machine, the TEMPLATE for four more effects), round 2 | the coordinator rewrote the plan from what ONE conversion taught and stated it for ALL: "every conversion pins return-mid-drain with `hasTail()` true and bit-identical output" (delay-shaped: the filter swap and the compressor need continuity, not identity, and following the rule literally restarts a fade, the click the swap exists to prevent; the gain has no such edge), and a bolded law, "enter does NOT reset what outlives the state", that the template's own `Off.enter()` contradicts | coordinator judgement (a lesson from one instance was generalised without checking it against the other instances it would govern; the same class as "a measured claim generalised beyond the dimension that was swept") | a rule written into a TEMPLATE is checked against every effect the template will be applied to, by reading each one's code, before it is stated as general; where the contract differs per effect the plan says so per effect. The round 2 brief asked the reviewers exactly that question, which is how it was caught before a copy was made |
| 2026-09-19 | Katalyst 5c-1, round 2 | a load-bearing line had no failing test behind it: `activeTail.reset()` in `Off.enter()` could be deleted with every row of both specs green, because the rows that mention the tail after a drain check it only in Off, where the answer is a hardcoded false. In a copy that is an orbit held long past its due, or for ever | test (documenting a rule is not guarding it; the step's own record named "the rows the next conversions copy" and the contract most easily broken in a copy was not among them) | for a template, the reviewer deletes each line of every `enter` in turn, by reading, and asks which row goes red; a line with no row gets one before the template is copied |
| 2026-09-19 | Katalyst 5c-1, round 3 | round 2's fix split the template's contract per effect, and the coordinator then wrote each effect's MECHANISM ("re-engaging reverses the fade from where it stands", a `ReleasingOut` that keeps "the gain-reduction coefficient") without those effects' files open: no fade-out exists in the code, the named state list has no home for a reversed ramp (followed literally it is the click it forbids), the three hosts of the filter swap cannot all honour it, and what a listener hears on re-entry is a design decision nobody had made | design (new audible behaviour written into a refactor plan as settled; the 5b-1 coordinator-judgement class once more) | a template states per-effect REQUIREMENTS only; a mechanism waits for that effect's own step, written with every host file open, and where it changes the sound it is decided with the maintainer and recorded in the task doc before the step is briefed. A fix round that ADDS claims to a plan is reviewed like code; the better fix removes claims |
| 2026-09-24 | phase 3 step 3d(i), round 1 | a new KDoc example and the music-writing skill described the built-in `sgbell` FM envelope as `adsr(0.001, 0.5, 0, 0)`; the preset has release 0.05 because release 0 "ticks on every note-off", so the popup and the skill agents copy from taught the clicking variant | checklist | `/dsl-design` checklist 10: an example that names a built-in quotes its values from the definition, never from memory |
| 2026-09-23/24 | phase 3 step 3d, the door-shape walk (coordinator) | the coordinator offered the maintainer door shapes that silently REVERSED two recorded decisions: `Fm.freq` exposed (a 2026-08-30 KDoc says deliberately hidden) and `dryFloor` renamed `floor` (the KDoc gives the reason: `floor()` is the round-down). The implementer's plan phase found both; the maintainer then kept one and reversed the other on the record | brief (coordinator) | before offering an option that changes a surface, grep the KDoc and the task docs for a recorded maintainer decision on that surface and show it WITH the question; a walk through many doors does this per door, not once |
| 2026-09-24 | phase 3 step 3d(ii), caught in the implementer's PLAN phase and then in round 1 | the coordinator wrote into section 3b, during the door-shape walk with the maintainer, that an omitted Katalyst door parameter "is unset and comes from the owner voice's slot, as every Katalyst knob does today": false for every stage (an omitted knob is the bare stage's fixed Constant; only a `Param` reads the orbit). The implementer refuted it for the phaser row; the reverb row kept the same sentence and BOTH blind reviewers found it (the round's one MAJOR); the corrected phaser row then overreached ("only classic()'s Param") | recurrence (the 2026-09-18 and 2026-09-19 rows: a claim about existing code verified by reading; the rule lived only in this ledger, where no author reads it while writing a plan) | the rule moved into `/review-loop` Rules, naming the design walk as the place it escapes, with "mark it UNVERIFIED until read"; the brief's "verify, do not implement" line is what caught the first copy, so it stays in every brief that carries a claim about current behaviour. And a correction sweeps EVERY copy of the sentence (the family lesson, applied to prose) |
| 2026-09-24 | phase 3 step 3d(ii), round 2 | the same false sentence survived in a THIRD copy, `/dsl-design` section 2 (the rule's one home), after the coordinator had corrected two copies and written "a correction sweeps EVERY copy" into the row above; round 2's one MAJOR | recurrence (the row above, the same day: the lesson was written and not executed) | the `/review-loop` rule now says HOW a correction is closed: a repo-wide grep for the claim's phrasing, run and recorded before the fix is reported. Run for this one: zero copies left |
| 2026-09-24 | phase 3 step 3d(iii), round 1 | the tail-only guards of the reordered sprudel doors (`wet != null || !(material != null || floor != null)`) could lose their NAME term with every suite green: every material-only call in the tests ran on a receiver with no numeric value (`note`, `s`), where the wrongly fired bare reinterpret writes nothing, so the misfire was unobservable; under that mutant `seq("0.3 3").body(material = "wood")` plays a raw wet of 3.0, on the path every migrated song call takes | recurrence (the 2026-09-20 family row, at a new grain: the TERMS of one boolean guard are a family too) | a guard built from OR/AND terms gets one row per term, each on an input where that term's misfire is OBSERVABLE (here a numeric receiver), and each term's deletion is a mutant; 8 terms across the three doors now each have a red row |
| 2026-09-25 | phase 3 step 5, round 1 | the slot-layer filter fill folded two meanings of a depth of 0 into one value: "unset" (the `SLOT_UNSET` sentinel, which a guard read back as 0) and "the author wrote 0", so an instrument whose depth default was 0 ("off") swept 7 semitones once a pattern wrote a stage knob, against the maintainer's decision that an authored default is never filled | design (a sentinel and a real value shared one representation at the point of decision) | a guard that must tell UNSET from a real value decides on the slot's declared DEFAULT (non-finite = unset) before anything resolves it to a number; a row per meaning (unset, authored 0, authored non-zero, written) |
| 2026-09-25 | phase 3 step 5, round 1 | the coordinator recorded a decision in the plan and briefed it, but left `/dsl-design` section 4, the rule's ONE home, saying the question was open and describing the old behaviour; the next agent would have "fixed" the shipped fill to match the stale home | recurrence (the one-home lesson of 2026-09-18, from the other side: a decision reaches the plan but not the rule's home) | when a decision changes a rule that has a one home, the home is edited in the SAME commit as the decision, before the step is briefed; the brief names the home |
| 2026-09-25 | phase 3 step 4, round 1 | the same day as the row above: D2 superseded a recorded decision (ledger W5 in `docs/plans/block-framing-invariance.md`, "the fused DistortIgnitor is DELETED"), and the coordinator updated W9 in that same table but not W5, so the record still called the new `FusedDistortIgnitor` a violated maintainer decision | recurrence (the lesson was written in the morning and not applied in the afternoon; the rule said "the rule's home", and a SUPERSEDED DECISION'S record is a home too) | when a decision supersedes a recorded one, the superseded record gets a SUPERSEDED line pointing at the new decision in the same commit as the decision, found by grepping the old decision's key words ("delete", its ledger id), not by remembering where it lives |
| 2026-09-25 | phase 3 step 5b (a1), round 1 | the stateless envelope core evaluated the attack-decay-sustain law at the gate frame for EVERY gate, including one at or before the onset (sprudel `legato(-0.1)`, raw through `VoiceFactory`): Square and SCurve squared a negative progress and a voice HEAD kept silent played a burst at 0 dBFS (about +120 dB with a zero attack); the old stateful latch had held 0.0, an implicit "0 before the onset" rule nobody had written down; the coordinator approved "stateless release start" without naming the law's domain | design (a stateless rewrite of a stateful evaluator dropped a rule the state had carried implicitly) | a brief that replaces state with a formula lists what the state's INITIAL value decided (here: silence before the first rendered frame) and makes each one an explicit rule with a row; the core now releases from 0 for a gate at or before the onset, pinned on six curves with the guard and its `<=` boundary mutation-checked |
| 2026-09-25 | phase 3 step 5b (a1), fix batch | an implementer stopped mid mutation run left mutant M05 applied to `EnvelopeCore.kt`; the tree looked like a "half-applied edit" and only the next implementer's audit against the cmp-verified backups found it | tooling (a mutation run has no cleanup when its agent dies) | a worker resumed after a stopped one first compares every production file its predecessor's mutation runner touched against that runner's backups, and every brief that allows mutation says "restore before you finish" |
